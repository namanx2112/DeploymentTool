name: Delete Artifacts

on:
  workflow_dispatch:

jobs:
  delete_artifacts:
    runs-on: windows-latest

    steps:
      - name: Delete all artifacts
        uses: actions/delete-artifact@v2
        with:
          name: '*'
